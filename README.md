# python -if-else-practice
A simple Python program that simulates an ATM withdrawal system using if-elif-else. It checks for insufficient balance, daily withdrawal limit, and successful transactions.
